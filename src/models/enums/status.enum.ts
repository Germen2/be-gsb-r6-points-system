export enum PlayerStatus {
  active = 'active',
  inactive = 'inactive',
}

export enum RewardStatus {
  active = 'active',
  inactive = 'inactive',
}

export enum ObjectiveStatus {
  active = 'active',
  inactive = 'inactive',
}
